function compara() {
    var campo = Number(document.getElementById("campo").value);
    var x =1;
    while(x <= campo){
        document.write(" AIA");
        document.write( x );
        x++;
    }
}
